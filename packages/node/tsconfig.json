{
  "extends": "../../tsconfig.lib.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"]
  },
  "references": [], // DON'T reference to DOM lib, because NODE and DOM types is conflict
  "include": ["src"],
  "exclude": [
    "**/*.test.*",
    "**/*.test-d.ts",
    "**/*.bench.*",
    "**/__tests__/**",
    "**/__mocks__/**",
    "**/__snapshots__/**"
  ]
}
